// Typed analytics event tracker — fleshed out in Epic A (Onboarding &
// Authentication), which is the first feature that needs signup_* and
// onboarding_* events. Placeholder export so the workspace resolves during
// the Step 1 monorepo restructure.
export const ANALYTICS_PACKAGE_READY = true
