# Repo-root env file — NOT read by either app. Workspace-level tooling only.
# See apps/web/.env.example and apps/admin/.env.example for the actual app secrets.

# Render Personal Access Token — read-only infra checks against the
# orphaned pshq-2.onrender.com service. Generate at Render Dashboard →
# Account Settings → API Keys. Not consumed by any app.
RENDER_API_KEY=
