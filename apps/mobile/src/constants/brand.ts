// Design Brief §3 palette — the real brand tokens (navy/cream/gold), as
// opposed to the ad hoc #3c87f7 blue used as an accent elsewhere in the
// app today. Scoped to the new Design Brief work (splash, welcome,
// dashboard visuals) for now rather than a global re-theme of every
// existing screen — that's a larger, separate pass.
export const Brand = {
  navy: '#16233F',
  cream: '#F6F2EA',
  gold: '#E8B923',
  hairline: '#E4DED2',
  mutedOnCream: '#5B6472',
  mutedOnNavy: '#C7CEDB',
} as const
